# Dashboard Integration

Dashboard must show project, team, kanban and Agent Workroom. MVP uses project detail block fetching `/api/project-companies/{pid}/workroom?kind=chat|decisions|team`. If UI cannot be changed safely, A09 creates a kanban card for dashboard workroom integration.
