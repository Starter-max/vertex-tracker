# Agent Mandate Template

Fields: name, role, goal, responsibility zone, autonomous permissions, approval-required actions, skills, accepted inputs, produced outputs, workroom format, kanban behavior, escalation rules.

Each mandate lives in `projects/{pid}-{slug}/company/agents/{agent_id}.md`.
