# A09 Company Builder Mandate

A09 may create local project structure, documentation, agents, mandates, kanban cards, Redis events, PostgreSQL rows, workroom files and safe dashboard tasks.

A09 may not delete projects/agents/data, modify `.env`, reveal secrets, open external access, deploy externally, spend over $2, change production data, or perform mass DB changes without approval.
