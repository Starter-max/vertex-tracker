import { AppState } from '@/lib/types';
const bucketStyles: Record<string, string> = { emerald: 'border-emerald-900 bg-emerald-950/30', amber: 'border-amber-900 bg-amber-950/30', slate: 'border-slate-800 bg-slate-900/70' };
export function BucketView({ state }: { state: AppState }) { const active = state.thoughts.filter(t=>t.category.startsWith('ACTIVE_')); const parking = state.thoughts.filter(t=>t.category==='PARKING'); const archive = state.thoughts.filter(t=>t.category==='ARCHIVE'); return <div className="grid gap-4 lg:grid-cols-3"><Bucket title="Активная работа" color="emerald" items={active}/><Bucket title="Парковка" color="amber" items={parking}/><Bucket title="Архив" color="slate" items={archive}/></div>; }
function Bucket({ title, color, items }: any) { return <section className={`rounded-3xl border ${bucketStyles[color]}`}><h2 className="text-xl font-medium">{title}</h2><div className="mt-3 space-y-2">{items.map((t:any)=><div key={t.id} className="rounded-2xl border border-slate-800 bg-slate-950 p-3 text-sm">{t.text}</div>)}{!items.length && <p className="text-sm text-slate-500">Пусто</p>}</div></section>; }
