'use client';
import { useMemo, useState } from 'react';
import { useAppState } from '@/lib/use-app-state';
export function VoiceCapture(){ const { state, setState } = useAppState(); const [text,setText]=useState(''); const [listening,setListening]=useState(false); const supports = useMemo(()=> typeof window !== 'undefined' && ('webkitSpeechRecognition' in window || 'SpeechRecognition' in window), []);
 const addThought=(value:string, source:'voice'|'text')=>{ if(!value.trim()) return; setState({...state, thoughts:[{id:crypto.randomUUID(), text:value.trim(), createdAt:new Date().toISOString(), category:'INBOX', source}, ...state.thoughts]}); setText(''); };
 const start=()=>{ const SR = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition; if(!SR){alert('Speech API not supported'); return;} const rec = new SR(); rec.lang='ru-RU'; rec.interimResults=true; rec.onresult=(e:any)=> setText(Array.from(e.results).map((r:any)=>r[0].transcript).join('')); rec.onend=()=>setListening(false); setListening(true); rec.start(); };
 return <section className="rounded-3xl border border-slate-800 bg-slate-900/70 p-5"><div className="flex min-h-[360px] flex-col justify-between gap-4"><button className="flex-1 rounded-[2rem] bg-emerald-600 text-2xl font-semibold shadow-lg" style={{minHeight:'40vh'}} onClick={start}>{listening ? 'Слушаю…' : 'Нажми и говори'}</button><textarea className="min-h-28 rounded-2xl border border-slate-700 bg-slate-950 p-4" value={text} onChange={e=>setText(e.target.value)} placeholder="Или напиши мысль"/><button className="rounded-2xl bg-slate-100 px-4 py-3 font-medium text-slate-950" onClick={()=>addThought(text,'text')}>Отправить в инбокс</button><p className="text-sm text-slate-400">{supports ? 'Web Speech API доступен' : 'Голосовой ввод недоступен в этом браузере'}</p></div></section>}
